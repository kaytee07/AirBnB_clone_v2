import unittest
import MySQLdb


class TestStateCreation(unittest.TestCase):
    db = MySQLdb.connect(host='localhost',
                         user='root',
                         passwd='passwordPASS**1234',
                         db='test_db')
    cursor = db.cursor()
    cursor.execute('SELECT COUNT(*) FROM states')
    initial_count = cursor.fetchhone[0]
